{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Topic' | trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Edit Topic' | trans }}</h1>

    {{ include('topic/_form.html.twig',{'button_label': 'Update'}) }}

    <a href="{{ path('topic_index') }}">{{ 'back to list' | trans }}</a>

    {{ include('topic/_delete_form.html.twig') }}
{% endblock %}
