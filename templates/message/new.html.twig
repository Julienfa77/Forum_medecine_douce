{% extends 'base.html.twig' %}

{% block title %}{{ 'New message' | trans }}{% endblock %}

{% block body %}
    <h1>Create new Message</h1>

    {{ include('message/_form.html.twig') }}

    <a href="{{ path('message_index') }}">{{ 'back to list' | trans }}</a>
{% endblock %}
